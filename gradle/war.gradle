apply plugin: "war"

bootWar {
    mainClassName = "com.cm.demandemisedisposition.DemandemisdispositionApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
